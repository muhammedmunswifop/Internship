print("11.What is the difference between a syntax error and a logical error?")
print("""
       -Syntax Error: Occurs when you break the grammatical rules of the language 
        (e.g. forgetting a closing parenthesis: print("Hello" ). The program won't run at all.

       -Logical Error: The program runs without crashing, but it produces the 
        wrong or unexpected output because the code's internal logic is wrong 
        (e.g. trying to add two numbers but accidentally using the minus '-' sign instead).""")
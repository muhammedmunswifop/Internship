print("12.What happens if indentation is incorrect in Python")
print(""" Python uses indentation (spaces/tabs) to define blocks of code instead of curly braces. 
        If your indentation is incorrect or inconsistent, Python will stop execution and 
        raise an \"IndentationError\"""")
